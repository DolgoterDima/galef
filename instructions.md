# Інструкції для AI

- **ВАЖЛИВО**: Не змінювати файл `variables.scss` (зокрема `src/scss/abstracts/_variables.scss`). Якщо є необхідність внести зміни до цього файлу, спочатку обов'язково повідомте про це та погодьте ці зміни зі мною.
- Всі класи станів (наприклад, `is-active`, `is-open` тощо) завжди пишіть як BEM-модифікатори (наприклад, `--active`, `--open`).
